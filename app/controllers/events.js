import Controller from '@ember/controller';

export default Controller.extend({
  actions: {
    clicked(event){
      alert("TODO: make this look better, with the What/When/Where/Why information.");
    }
  }
});
